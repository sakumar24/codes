#include<stdio.h>
#include<string.h>

int main()
{
	char str1[10];
	char str2[10];

	printf("str1: "); scanf("%s",str1);
	printf("str2: "); scanf("%s",str2);
	
	char* temp = strcat(str1,str1);
	char* ptr = strstr(temp,str2);

	if(ptr == NULL)
		printf("NO\n");
	else
		printf("YES\n");


return 0;
}
